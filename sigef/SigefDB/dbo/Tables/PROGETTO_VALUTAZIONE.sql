CREATE TABLE [dbo].[PROGETTO_VALUTAZIONE] (
    [ID]            INT           IDENTITY (1, 1) NOT NULL,
    [ID_PROGETTO]   INT           NOT NULL,
    [ID_VARIANTE]   INT           NULL,
    [ID_FILE]       INT           NULL,
    [ORDINE_FIRMA]  INT           CONSTRAINT [DF_PROGETTO_VALUTAZIONE_ORDINE_FIRMA] DEFAULT ((0)) NOT NULL,
    [SEGNATURA]     VARCHAR (100) NULL,
    [ID_NOTE]       INT           NULL,
    [OPERATORE]     INT           NOT NULL,
    [DATA_MODIFICA] DATETIME      NOT NULL,
    [ANNULLATO]     BIT           CONSTRAINT [DF_PROGETTO_VALUTAZIONE_ANNULLATO] DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_PROGETTO_VALUTAZIONE] PRIMARY KEY CLUSTERED ([ID] ASC),
    CONSTRAINT [FK_PROGETTO_VALUTAZIONE_PROGETTO] FOREIGN KEY ([ID_PROGETTO]) REFERENCES [dbo].[PROGETTO] ([ID_PROGETTO]),
    CONSTRAINT [FK_PROGETTO_VALUTAZIONE_VARIANTI] FOREIGN KEY ([ID_VARIANTE]) REFERENCES [dbo].[VARIANTI] ([ID_VARIANTE])
);

