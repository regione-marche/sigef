CREATE TABLE [dbo].[PIANO_INVESTIMENTI_BK_VARIANTE] (
    [ID]                   INT             IDENTITY (1, 1) NOT NULL,
    [ID_INVESTIMENTO]      INT             NOT NULL,
    [CONTRIBUTO_RICHIESTO] DECIMAL (18, 2) NOT NULL,
    [DATA]                 DATETIME        NOT NULL,
    CONSTRAINT [PK_PIANO_INVESTIMENTI_BK_VARIANTE] PRIMARY KEY CLUSTERED ([ID] ASC) WITH (FILLFACTOR = 80)
);

