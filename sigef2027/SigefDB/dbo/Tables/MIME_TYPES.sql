CREATE TABLE [dbo].[MIME_TYPES] (
    [ESTENSIONE] VARCHAR (100) NOT NULL,
    [MIME_TYPE]  VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_MIME_TYPE] PRIMARY KEY CLUSTERED ([ESTENSIONE] ASC) WITH (FILLFACTOR = 100)
);

